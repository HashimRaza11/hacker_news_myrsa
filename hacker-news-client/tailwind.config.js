// tailwind.config.js

module.exports = {
  content: [
      './src/**/*.{js,jsx,ts,tsx}',  // Ensure all JS and TS files in src are included
      './public/index.html'           // Include the public HTML file
  ],
  theme: {
      extend: {},
  },
  plugins: [],
}
